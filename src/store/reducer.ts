const reducer = (state: any, action: any) => {
  const { type, payload } = action;

  switch (type) {
    case 'a':
      
      return state
  
    default:
      return state
  }


};

export default reducer;
